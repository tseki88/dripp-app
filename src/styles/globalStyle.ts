import {StyleSheet} from 'react-native';

const globalStyle = StyleSheet.create({
  wrapper: {
    // margin: 5,
    padding: 5,
    flex: 1,
    // borderWidth: 1,
  },
});

export default globalStyle;
